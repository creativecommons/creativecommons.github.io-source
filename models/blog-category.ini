[model]
name = Blog Category
label = {{ this.name }}
hidden = yes

[children]
replaced_with = site.query('archives/' + this.parent.path.split('/')[-1]).filter(F.categories.contains(this)) 

[fields.name]
label = Name
type = string
